apiVersion: apps/v1
kind: Deployment
metadata:
  name: link-service
  namespace: url-shortener
  labels:
    app: link-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: link-service
  template:
    metadata:
      labels:
        app: link-service
      annotations:
        # Tells OTel Collector k8sattributes processor to enrich spans with pod metadata
        instrumentation.opentelemetry.io/inject-go: "true"
    spec:
      containers:
        - name: link-service
          image: piyushsachdeva/url-shortener-link:otel   # rebuild tag after applying patches
          imagePullPolicy: Always
          env:
            - name: DB_HOST
              value: postgres.url-shortener.svc.cluster.local
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: database
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: password
            - name: REDIS_HOST
              value: redis.url-shortener.svc.cluster.local
            - name: SERVICE_PORT
              value: "8001"
            # OTel SDK configuration — points to in-cluster collector
            - name: OTEL_SERVICE_NAME
              value: link-service
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.url-shortener.svc.cluster.local:4318
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: http/protobuf
            - name: DEPLOYMENT_ENV
              value: production
          ports:
            - containerPort: 8001
              name: http
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 5
      initContainers:
        - name: wait-for-postgres
          image: postgres:15-alpine
          command:
            - sh
            - -c
            - until pg_isready -h postgres.url-shortener.svc.cluster.local -p 5432 -U postgres; do sleep 2; done
---
apiVersion: v1
kind: Service
metadata:
  name: link-service
  namespace: url-shortener
spec:
  selector:
    app: link-service
  ports:
    - port: 8001
      targetPort: 8001
      name: http
